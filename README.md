# Ciudad Patrimonio Mundial (CPM) 

### Comando para ejecución de la aplicación

ng serve --proxy-config proxy.conf.json

### Comando para guardar libros en csv

npm install --save-dev @types/file-saver
